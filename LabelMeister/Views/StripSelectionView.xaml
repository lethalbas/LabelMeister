<UserControl x:Class="LabelMeister.Views.StripSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16" Background="{StaticResource DarkBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Strip Selection" 
                   FontSize="24"
                   FontWeight="SemiBold"
                   Margin="0,0,0,16"/>

        <Border Grid.Row="1" 
                Padding="16" 
                Margin="0,0,0,16"
                Background="{StaticResource DarkSurfaceElevated}"
                BorderBrush="{StaticResource DarkBorder}"
                BorderThickness="1"
                CornerRadius="4">
            <StackPanel IsEnabled="{Binding IsEnabled}">
                <Grid Margin="0,0,0,16">
                    <ComboBox x:Name="StripSizeComboBox"
                              ItemsSource="{Binding AvailableStrips}"
                              SelectedItem="{Binding SelectedStrip, Mode=TwoWay}"
                              DisplayMemberPath="Name"
                              SelectionChanged="ComboBox_SelectionChanged"
                              PreviewMouseLeftButtonDown="ComboBox_PreviewMouseLeftButtonDown"
                              IsEnabled="{Binding IsEnabled}">
                        <ComboBox.Style>
                            <Style TargetType="ComboBox">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsCustomStrip}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ComboBox.Style>
                        <ComboBox.ToolTip>
                            <ToolTip Content="Select Strip Size"/>
                        </ComboBox.ToolTip>
                    </ComboBox>
                    <TextBox Text="Custom"
                             IsReadOnly="True"
                             Background="{StaticResource DarkSurfaceElevated}"
                             Foreground="{StaticResource DarkTextSecondary}"
                             BorderBrush="{StaticResource DarkBorder}"
                             Padding="8,4"
                             VerticalContentAlignment="Center"
                             IsEnabled="False">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsCustomStrip}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </Grid>

                <CheckBox IsChecked="{Binding IsCustomStrip, Mode=TwoWay}"
                          Content="Use Custom Dimensions"
                          Margin="0,0,0,16"
                          IsEnabled="{Binding IsEnabled}"/>

                <StackPanel Orientation="Horizontal">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsCustomStrip}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <TextBlock Text="Width (mm):" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox Text="{Binding CustomWidth, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="100" Margin="0,0,16,0" IsEnabled="{Binding IsEnabled}"/>
                    <TextBlock Text="Height (mm):" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox Text="{Binding CustomHeight, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="100" Margin="0,0,16,0" IsEnabled="{Binding IsEnabled}"/>
                    <CheckBox IsChecked="{Binding IsLandscape, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Content="Landscape" VerticalAlignment="Center" IsEnabled="{Binding IsEnabled}"/>
                </StackPanel>

                <CheckBox Content="Mark as completed" 
                          IsChecked="{Binding IsCompleted, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          Margin="0,16,0,0"
                          IsEnabled="{Binding IsEnabled}"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" 
                Padding="16"
                Background="{StaticResource DarkSurfaceElevated}"
                BorderBrush="{StaticResource DarkBorder}"
                BorderThickness="1"
                CornerRadius="4">
            <Grid>
                <Rectangle Fill="{StaticResource DarkBorder}"
                          Stroke="{StaticResource DarkBorder}"
                          StrokeThickness="2"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          x:Name="PreviewRectangle"
                          MinWidth="50"
                          MinHeight="50"
                          MaxWidth="400"
                          MaxHeight="400"/>
                <TextBlock Text="{Binding PreviewText}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="12"
                           Foreground="{StaticResource DarkTextSecondary}"
                           Margin="0,0,0,-50"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>

